import 'package:flutter/material.dart';

class Alet extends StatelessWidget {
  const Alet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Abdul Rafay"),
      ),
      body: Container(
        decoration: BoxDecoration(),
        child: Text("Abdul"),
      ),
    );
  }
}
